---
import Layout from '../components/Layout.astro';
import { site } from '../config/site';
import { getHomeFeaturedProjects } from '../utils/content';

const systems = await getHomeFeaturedProjects();

const process = [
  {
    title: 'Observe',
    description:
      'Understand how the work really happens, including repeated friction, unofficial workarounds, missing context and unclear ownership.',
  },
  {
    title: 'Simplify',
    description:
      'Reduce the process to its essential inputs, decisions, actions, hand-offs and exceptions.',
  },
  {
    title: 'Build',
    description:
      'Ship the smallest complete system people can use in real work, not just a demonstration of the underlying AI.',
  },
  {
    title: 'Iterate',
    description:
      'Watch where it fails, learn from edge cases and strengthen the workflow with clearer rules, human review and better feedback.',
  },
];

const skills = [
  {
    group: 'Workflow design',
    items: [
      'Process and constraint mapping',
      'Information architecture',
      'Human-in-the-loop flows',
      'Edge cases and escalation paths',
    ],
  },
  {
    group: 'Applied AI',
    items: [
      'LLM-assisted workflows',
      'Structured outputs',
      'Retrieval and knowledge systems',
      'Tool use and orchestration',
    ],
  },
  {
    group: 'Product engineering',
    items: [
      'TypeScript, Next.js and Astro',
      'Supabase and data modelling',
      'APIs, webhooks and integrations',
      'Deployment and monitoring',
    ],
  },
];
---

<Layout>
  <div class="mock">
    <section class="mock-hero" aria-labelledby="mock-hero-title">
      <div class="mock-shell mock-hero__grid">
        <div class="mock-hero__copy" data-reveal data-reveal-hero>
          <p class="mock-kicker">Applied AI · Workflow automation · Product engineering</p>
          <h1 id="mock-hero-title">Practical AI<br />Systems for<br />Messy Manual<br />Workflows.</h1>
          <p class="mock-hero__intro">
            I map how work gets done, identify the real constraint, and build AI-enabled workflows, internal tools and automation around it.
          </p>
          <div class="mock-actions">
            <a class="mock-button mock-button--primary" href="#systems">See what I've built</a>
            <a class="mock-text-link" href="#process">How I work <span aria-hidden="true">↓</span></a>
          </div>
        </div>

        <figure class="mock-refinement" data-reveal data-reveal-hero data-reveal-hero-media>
          <img
            src="/images/neural-system-hero.png"
            alt="An intricate spherical network of connected nodes and paths"
            width="1254"
            height="1254"
            loading="eager"
            decoding="async"
          />
        </figure>
      </div>
    </section>

    <!-- SYSTEMS: LOOP as feature, supporting projects compact -->
    <section id="systems" class="mock-section mock-systems" aria-labelledby="systems-title">
      <div class="mock-shell">
        <header class="mock-systems__head" data-reveal>
          <p class="mock-kicker">Systems</p>
          <h2 id="systems-title">AI systems built around real workflows.</h2>
          <p class="mock-systems__lede">
            Ambiguous manual workflows turned into systems people can actually run.
          </p>
        </header>

        {systems[0] && (
          <article class="mock-feature" data-reveal data-reveal-media>
            <a href={`/projects/${systems[0].slug}`} aria-label={`Read the ${systems[0].data.title} case study`}>
              <div class="mock-feature__visual">
                {(systems[0].data.cardImage ?? systems[0].data.coverImage) ? (
                  <img
                    src={systems[0].data.cardImage ?? systems[0].data.coverImage}
                    alt={`${systems[0].data.title} interface`}
                    loading="lazy"
                    decoding="async"
                    style={`object-position: ${systems[0].data.coverPosition === 'top' ? 'left top' : (systems[0].data.coverPosition ?? 'left top')};`}
                  />
                ) : null}
              </div>
              <div class="mock-feature__copy">
                <div class="mock-feature__meta">
                  <span>{systems[0].data.category}</span>
                  <span>{systems[0].data.status}</span>
                </div>
                <h3>{systems[0].data.title}</h3>
                <p>{systems[0].data.description}</p>
                {systems[0].data.systemLine && (
                  <p class="mock-feature__system-line">{systems[0].data.systemLine}</p>
                )}
                <span class="mock-feature__view">
                  Case study <span aria-hidden="true">↗</span>
                </span>
              </div>
            </a>
          </article>
        )}

        <div class="mock-pair" data-reveal-stagger>
          {systems.slice(1).map((project) => {
            if (!project) return null;
            const {
              title,
              description,
              coverImage,
              cardImage,
              coverFit,
              coverPosition,
              category,
              status,
            } = project.data;
            const thumb = cardImage ?? coverImage;
            return (
              <article class="mock-pair__item" data-reveal data-reveal-media>
                <a href={`/projects/${project.slug}`} aria-label={`Read the ${title} case study`}>
                  <div class={`mock-pair__visual${coverFit === 'contain' ? ' mock-pair__visual--contain' : ''}`}>
                    {thumb ? (
                      <img
                        src={thumb}
                        alt={`${title} interface`}
                        loading="lazy"
                        decoding="async"
                        style={`object-fit: ${coverFit ?? 'cover'}; object-position: ${coverPosition ?? 'center top'};`}
                      />
                    ) : (
                      <span>{title}</span>
                    )}
                  </div>
                  <div class="mock-pair__copy">
                    <div class="mock-pair__meta">
                      <span>{category}</span>
                      <span>{status}</span>
                    </div>
                    <h3>{title}</h3>
                    <p>{description}</p>
                    <span class="mock-pair__view">
                      Case study <span aria-hidden="true">↗</span>
                    </span>
                  </div>
                </a>
              </article>
            );
          })}
        </div>

        <div class="mock-section-action" data-reveal>
          <a class="mock-text-link" href="/projects">View all systems <span aria-hidden="true">→</span></a>
        </div>
      </div>
    </section>

    <!-- ABOUT + CAPABILITIES: one paper surface -->
    <section id="about" class="mock-section mock-about" aria-labelledby="about-title">
      <div class="mock-shell">
        <div class="mock-about__layout">
          <figure class="mock-about__photo" data-reveal>
            <div class="mock-about__frame">
              <img
                src="/images/headshot-studio-tight.png?v=12"
                alt={`${site.name} portrait`}
                width="819"
                height="1024"
                loading="lazy"
                decoding="async"
              />
            </div>
          </figure>

          <div class="mock-about__copy" data-reveal data-reveal-delay="1">
            <p class="mock-kicker">About</p>
            <h2 id="about-title">I turn messy operational work into systems people can run.</h2>
            <p>
              Ambiguous process, scattered tools, context stuck in people’s heads — that’s usually where I start.
            </p>
            <p>
              I map the real workflow, then build the capture, automation, and interfaces around it: product thinking, applied AI, and full-stack implementation in one pass.
            </p>
            <p>
              I care less about whether it looks agentic and more about whether someone can complete the work reliably.
            </p>
          </div>
        </div>

        <div id="skills" class="mock-about__capabilities" aria-labelledby="skills-title">
          <header data-reveal>
            <p class="mock-kicker">Capabilities</p>
            <h2 id="skills-title">I build the system around the model.</h2>
            <p>
              The useful part of applied AI is rarely the prompt alone. It is the workflow, data, interface, integration, guardrails and feedback loop that make it dependable.
            </p>
          </header>
          <div class="mock-skills__groups" data-reveal-stagger>
            {skills.map((skill) => (
              <div class="mock-skill-group" data-reveal>
                <p class="mock-kicker">{skill.group}</p>
                <ul>
                  {skill.items.map((item) => (
                    <li>{item}</li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    <!-- PROCESS -->
    <section id="process" class="mock-section mock-process" aria-labelledby="process-title">
      <div class="mock-shell mock-process__layout">
        <header class="mock-process__intro" data-reveal>
          <p class="mock-kicker">How I work</p>
          <h2 id="process-title">Refinement is the work.</h2>
          <p>
            I rarely start with a clean specification. I start with scattered information, repeated friction and a problem that has not been properly framed yet.
          </p>
        </header>
        <ol class="mock-process__steps" data-reveal-stagger>
          {process.map((step, index) => (
            <li data-reveal>
              <span>0{index + 1}</span>
              <div>
                <h3>{step.title}</h3>
                <p>{step.description}</p>
              </div>
            </li>
          ))}
        </ol>
      </div>
    </section>

    <!-- CONTACT: job-seeking framing -->
    <section id="contact" class="mock-contact" aria-labelledby="contact-title">
      <div class="mock-shell mock-contact__inner">
        <div data-reveal>
          <p class="mock-kicker">Work with me</p>
          <h2 id="contact-title">Looking for an applied AI builder who can work from ambiguity to production?</h2>
        </div>
        <div class="mock-contact__copy" data-reveal data-reveal-delay="1">
          <p>
            I map real workflows, build the tools and integrations around them, and refine them through real use. I’m open to applied AI engineering roles where I can do that work from inside the team.
          </p>
          <div class="mock-contact__actions">
            <a class="mock-button mock-button--light" href={`mailto:${site.email}`}>
              Email me <span aria-hidden="true">↗</span>
            </a>
            <a class="mock-text-link mock-text-link--on-dark" href={`mailto:${site.email}`}>{site.email}</a>
          </div>
        </div>
      </div>
    </section>
  </div>

  <script>
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const reveals = Array.from(document.querySelectorAll('[data-reveal]'));

    const show = (el) => el.classList.add('is-in');

    if (reduceMotion || !('IntersectionObserver' in window)) {
      reveals.forEach(show);
    } else {
      document.querySelectorAll('[data-reveal-hero]').forEach((el) => {
        window.setTimeout(() => show(el), 80);
      });

      const observer = new IntersectionObserver(
        (entries) => {
          for (const entry of entries) {
            if (!entry.isIntersecting) continue;
            show(entry.target);
            observer.unobserve(entry.target);
          }
        },
        { threshold: 0.12, rootMargin: '0px 0px -8% 0px' },
      );

      reveals.forEach((el) => {
        if (el.hasAttribute('data-reveal-hero')) return;
        observer.observe(el);
      });
    }
  </script>
</Layout>

<style>
  .mock {
    --paper: #f4f0e8;
    --raised: #faf7f1;
    --deep: #e9e2d7;
    --ink: #1d1f1e;
    --soft: #454744;
    --muted: #77756f;
    --line: rgba(48, 47, 43, 0.14);
    --line-strong: rgba(48, 47, 43, 0.24);
    --signal: #3d5a4c;
    --grain: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    color: var(--ink);
    background: var(--paper);
    font-family: 'IBM Plex Sans', system-ui, sans-serif;
  }

  .mock a {
    text-decoration: none;
  }

  .mock a:not(.mock-button) {
    color: inherit;
  }

  .mock-shell {
    width: min(92rem, calc(100% - 3rem));
    margin-inline: auto;
  }

  .mock-kicker {
    margin: 0;
    color: var(--muted);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  /* Hero */
  .mock-hero {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    min-height: calc(100svh - 6.4rem);
    display: flex;
    flex-direction: column;
    background: var(--paper);
  }

  .mock-hero::before,
  .mock-hero::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .mock-hero::before {
    z-index: 0;
    background:
      radial-gradient(ellipse 38% 52% at 74% 40%, rgba(190, 128, 73, 0.22), transparent 68%),
      radial-gradient(ellipse 34% 46% at 15% 73%, rgba(50, 73, 77, 0.13), transparent 70%),
      radial-gradient(ellipse 29% 36% at 34% 24%, rgba(255, 255, 255, 0.72), transparent 68%),
      radial-gradient(ellipse 30% 42% at 91% 84%, rgba(118, 95, 72, 0.12), transparent 72%),
      linear-gradient(118deg, rgba(255, 255, 255, 0.4), transparent 46%);
  }

  .mock-hero::after {
    z-index: 2;
    opacity: 1;
    background-image:
      repeating-linear-gradient(7deg, rgba(75, 62, 48, 0.038) 0, rgba(75, 62, 48, 0.038) 1px, transparent 1px, transparent 4px),
      repeating-linear-gradient(97deg, rgba(255, 255, 255, 0.1) 0, rgba(255, 255, 255, 0.1) 1px, transparent 1px, transparent 9px);
    mix-blend-mode: multiply;
  }

  .mock-hero__grid {
    flex: 1;
    position: relative;
    display: flex;
    align-items: center;
    width: min(92rem, calc(100% - 3rem));
    max-width: 92rem;
    margin-inline: auto;
    padding-block: clamp(4.5rem, 8vh, 7rem) clamp(3rem, 5vh, 4rem);
  }

  .mock-hero__copy {
    position: relative;
    z-index: 3;
    max-width: min(44rem, 48%);
  }

  .mock-hero h1 {
    margin: 1.25rem 0 0;
    font-size: clamp(3.6rem, 4.8vw + 1.2rem, 6.1rem);
    font-weight: 500;
    line-height: 0.98;
    letter-spacing: -0.065em;
  }

  .mock-hero__intro {
    max-width: 42rem;
    margin: 1.8rem 0 0;
    color: #263b47;
    font-size: clamp(1.05rem, 1.6vw, 1.35rem);
    font-weight: 400;
    line-height: 1.65;
    text-wrap: balance;
  }

  .mock-actions {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 1.65rem;
    margin-top: 2.75rem;
  }

  .mock-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    min-height: 3.65rem;
    padding: 0.95rem 1.45rem;
    border-radius: 0.35rem;
    font-size: 0.98rem;
    font-weight: 500;
    transition: transform 220ms cubic-bezier(0.22, 1, 0.36, 1), box-shadow 220ms ease;
  }

  .mock-button:hover {
    transform: translateY(-2px);
  }

  .mock-button:active {
    transform: translateY(1px) scale(0.985);
  }

  a.mock-button.mock-button--primary {
    color: var(--paper);
    background: var(--ink);
    box-shadow: 0 8px 22px rgba(31, 31, 28, 0.12);
  }

  a.mock-button.mock-button--light {
    color: #202220;
    background: #f4f0e8;
  }

  .mock-text-link {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--soft);
    font-size: 0.98rem;
    font-weight: 500;
  }

  .mock-text-link span {
    transition: transform 180ms ease;
  }

  .mock-text-link:hover {
    color: var(--ink);
  }

  .mock-text-link:hover span {
    transform: translate(2px, -1px);
  }

  .mock-text-link--on-dark {
    color: color-mix(in srgb, #f4f0e8 72%, transparent);
  }

  .mock-text-link--on-dark:hover {
    color: #f4f0e8;
  }

  .mock-refinement {
    position: absolute;
    left: auto;
    right: -1.5%;
    top: 53%;
    width: min(54%, 48rem);
    height: auto;
    aspect-ratio: 1;
    transform: translateY(-50%);
    margin: 0;
    z-index: 1;
    pointer-events: none;
    overflow: visible;
  }

  .mock-refinement img {
    position: static;
    width: 100%;
    max-width: 100%;
    height: 100%;
    display: block;
    margin: 0;
    object-fit: contain;
    object-position: center;
    mix-blend-mode: darken;
    opacity: 0.98;
    -webkit-mask-image: radial-gradient(circle, #000 68%, rgba(0, 0, 0, 0.72) 83%, transparent 100%);
    mask-image: radial-gradient(circle, #000 68%, rgba(0, 0, 0, 0.72) 83%, transparent 100%);
  }

  .mock-hero__foot {
    position: relative;
    z-index: 3;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding-block: 1.1rem;
    color: var(--muted);
    border-top: 1px solid var(--line);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  /* Shared section chrome */
  .mock-section {
    padding-block: clamp(5.5rem, 10vw, 9rem);
    border-top: 1px solid var(--line);
  }

  .mock-section-head {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(16rem, 0.75fr);
    gap: clamp(2rem, 7vw, 8rem);
    align-items: end;
    margin-bottom: clamp(3rem, 6vw, 5rem);
  }

  .mock-section-head h2,
  .mock-about__copy h2,
  .mock-about__capabilities h2,
  .mock-process__intro h2,
  .mock-contact h2 {
    margin: 0.85rem 0 0;
    font-size: clamp(2.2rem, 4.2vw, 4.2rem);
    font-weight: 400;
    line-height: 1.02;
    letter-spacing: -0.055em;
    text-wrap: balance;
  }

  .mock-section-head > p,
  .mock-about__copy > p:not(.mock-kicker),
  .mock-about__capabilities header > p:not(.mock-kicker),
  .mock-process__intro > p:last-child,
  .mock-contact__copy > p {
    margin: 0;
    color: var(--muted);
    font-size: 1rem;
    font-weight: 300;
    line-height: 1.7;
  }

  /* About */
  .mock-about {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    /* Soft warm paper mid-tone — closer to site palette, not brown */
    background:
      radial-gradient(ellipse 60% 50% at 90% 8%, rgba(48, 47, 43, 0.04) 0%, transparent 65%),
      linear-gradient(165deg, #f3eee5 0%, #ebe5da 55%, #e5dfd3 100%);
  }

  .mock-about::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.07;
    background-image: var(--grain);
    background-size: 140px 140px;
    mix-blend-mode: multiply;
  }

  .mock-about > * {
    position: relative;
    z-index: 1;
  }

  .mock-about__layout {
    display: grid;
    grid-template-columns: minmax(16rem, 0.95fr) minmax(22rem, 1.05fr);
    gap: clamp(1.5rem, 5vw, 4.5rem);
    align-items: center;
  }

  .mock-about__photo {
    margin: 0;
    max-width: min(26rem, 100%);
    width: 100%;
    justify-self: start;
    align-self: center;
  }

  .mock-about__frame {
    position: relative;
    overflow: hidden;
    aspect-ratio: 4 / 5;
    border-radius: 0.15rem;
    background: #8f857c;
    box-shadow:
      inset 0 0 0 1px rgba(48, 47, 43, 0.14),
      0 0.65rem 1.6rem rgba(48, 47, 43, 0.14);
    line-height: 0;
  }

  .mock-about__photo img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: center 10%;
  }

  .mock-about__photo figcaption {
    margin-top: 0.75rem;
    text-align: left;
    color: var(--muted);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .mock-about__copy {
    padding-top: 0.35rem;
  }

  .mock-about__copy h2 {
    max-width: 18ch;
    margin-bottom: 1.5rem;
  }

  .mock-about__copy > p + p {
    margin-top: 1rem;
  }

  .mock-about__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.85rem 1.5rem;
    margin-top: 1.35rem;
    padding-top: 0;
    border-top: none;
    color: var(--muted);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .mock-about__meta a {
    color: var(--ink);
    font-family: 'IBM Plex Sans', system-ui, sans-serif;
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0;
    text-transform: none;
  }

  .mock-about__meta a span {
    display: inline-block;
    transition: transform 180ms ease;
  }

  .mock-about__meta a:hover span {
    transform: translateX(3px);
  }

  .mock-about__capabilities {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(22rem, 1.1fr);
    gap: clamp(2rem, 5vw, 4.5rem);
    align-items: start;
    margin-top: clamp(2.75rem, 5vw, 4rem);
    padding-top: clamp(2.25rem, 4vw, 3.25rem);
    border-top: 1px solid color-mix(in srgb, var(--line) 88%, transparent);
  }

  .mock-about__capabilities header > p:not(.mock-kicker) {
    max-width: 32rem;
    margin-top: 1.1rem;
  }

  .mock-about__capabilities h2 {
    max-width: 16ch;
  }

  .mock-section-action {
    display: flex;
    justify-content: flex-end;
    margin-top: 2.25rem;
  }

  /* Systems — LOOP feature + compact supporting projects */
  .mock-systems {
    background: linear-gradient(180deg, #f7f4ee 0%, #f2eee6 100%);
  }

  .mock-systems__head {
    margin-bottom: clamp(3.75rem, 7.5vw, 6rem);
  }

  .mock-systems__head h2 {
    max-width: 16ch;
    margin: 0.75rem 0 0;
    font-size: clamp(2.2rem, 4.2vw, 4.2rem);
    font-weight: 400;
    line-height: 1.02;
    letter-spacing: -0.055em;
    text-wrap: balance;
  }

  .mock-systems__lede {
    max-width: 34rem;
    margin: 0.85rem 0 0;
    color: var(--muted);
    font-size: 1.02rem;
    font-weight: 300;
    line-height: 1.6;
  }

  .mock-feature {
    margin-bottom: clamp(3.25rem, 6vw, 5rem);
  }

  .mock-feature > a {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) minmax(16rem, 0.75fr);
    gap: clamp(1.75rem, 4vw, 3.5rem);
    align-items: start;
  }

  .mock-feature__visual {
    position: relative;
    overflow: hidden;
    width: 100%;
    aspect-ratio: 16 / 10;
    min-height: 0;
    background: #0a0a0a;
    border-radius: 0.4rem;
    box-shadow:
      inset 0 0 0 1px rgba(48, 47, 43, 0.08),
      0 0.8rem 2rem rgba(48, 47, 43, 0.055);
  }

  .mock-feature__visual img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: left top;
    image-rendering: -webkit-optimize-contrast;
    filter: contrast(1.04);
    transition: transform 700ms cubic-bezier(0.22, 1, 0.36, 1), filter 300ms ease;
  }

  .mock-feature:hover .mock-feature__visual img {
    transform: scale(1.03);
    filter: contrast(1.05);
  }

  .mock-feature__copy {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    padding-top: 0.2rem;
  }

  .mock-feature__meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 1.25rem;
    color: var(--muted);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .mock-feature__copy h3 {
    margin: 1.1rem 0 0;
    font-size: clamp(2rem, 3.4vw, 3.4rem);
    font-weight: 400;
    letter-spacing: -0.05em;
    line-height: 1.02;
  }

  .mock-feature__copy > p:not(.mock-feature__system-line) {
    max-width: 28rem;
    margin: 1.1rem 0 0;
    color: var(--muted);
    font-size: 0.95rem;
    font-weight: 300;
    line-height: 1.65;
  }

  .mock-feature__copy > p.mock-feature__system-line {
    margin: 1rem 0 0;
    color: var(--ink);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.68rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .mock-feature__view,
  .mock-pair__view {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    margin-top: 1.6rem;
    font-size: 0.82rem;
    font-weight: 500;
  }

  .mock-feature__view span,
  .mock-pair__view span {
    transition: transform 180ms ease;
  }

  .mock-feature:hover .mock-feature__view span,
  .mock-pair__item:hover .mock-pair__view span {
    transform: translate(2px, -2px);
  }

  .mock-pair {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(1.35rem, 3vw, 2.25rem);
  }

  .mock-pair__item > a {
    display: grid;
    gap: 0.7rem;
  }

  .mock-pair__visual {
    overflow: hidden;
    aspect-ratio: 16 / 10;
    background: #0a0a0a;
    border-radius: 0.4rem;
    box-shadow:
      inset 0 0 0 1px rgba(48, 47, 43, 0.07),
      0 0.55rem 1.4rem rgba(48, 47, 43, 0.045);
  }

  .mock-pair__visual--contain {
    display: grid;
    place-items: center;
  }

  .mock-pair__visual img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: center top;
  }

  .mock-pair__visual--contain img {
    width: 100%;
    height: 100%;
  }

  .mock-pair__meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem 0.9rem;
    color: var(--muted);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.54rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .mock-pair__copy h3 {
    margin: 0.25rem 0 0;
    font-size: clamp(1rem, 1.4vw, 1.15rem);
    font-weight: 500;
    letter-spacing: -0.025em;
    line-height: 1.2;
  }

  .mock-pair__copy > p {
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
    overflow: hidden;
    margin: 0.35rem 0 0;
    color: var(--muted);
    font-size: 0.8rem;
    font-weight: 300;
    line-height: 1.45;
  }

  .mock-pair__view {
    margin-top: 0.4rem;
    font-size: 0.76rem;
  }

  /* Capability columns (now nested under About) */
  .mock-skills__groups {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(1.5rem, 3vw, 2.75rem);
  }

  .mock-skill-group {
    padding-top: 0;
  }

  .mock-skill-group .mock-kicker {
    margin: 0 0 0.15rem;
    padding-bottom: 0;
    border-bottom: none;
    color: var(--ink);
  }

  .mock-skill-group ul {
    display: grid;
    gap: 0.7rem;
    margin: 0.85rem 0 0;
    padding: 0;
    list-style: none;
  }

  .mock-skill-group li {
    color: var(--soft);
    font-size: 0.95rem;
    font-weight: 300;
    line-height: 1.35;
  }

  /* Process — raised cool band after the combined About surface */
  .mock-process {
    position: relative;
    isolation: isolate;
    /* overflow must stay visible so .mock-process__intro sticky works */
    background:
      linear-gradient(180deg, rgba(255, 252, 247, 0.85) 0%, transparent 32%),
      var(--raised);
  }

  .mock-process::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.045;
    background-image: var(--grain);
    background-size: 160px 160px;
    mix-blend-mode: multiply;
  }

  .mock-process > * {
    position: relative;
    z-index: 1;
  }

  .mock-process__layout {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(24rem, 1.2fr);
    gap: clamp(3rem, 8vw, 9rem);
    align-items: start;
  }

  .mock-process__intro {
    position: sticky;
    top: 5rem;
  }

  .mock-process__intro h2 {
    max-width: 9ch;
  }

  .mock-process__intro > p:last-child {
    max-width: 32rem;
    margin-top: 1.4rem;
  }

  .mock-process__steps {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .mock-process__steps li {
    display: grid;
    grid-template-columns: 3.5rem 1fr;
    gap: 1.25rem;
    padding-block: 2rem;
    border-bottom: 1px solid var(--line);
  }

  .mock-process__steps li:last-child {
    border-bottom: none;
  }

  .mock-process__steps span {
    padding-top: 0.35rem;
    color: var(--signal);
    font-family: 'IBM Plex Mono', ui-monospace, monospace;
    font-size: 0.65rem;
  }

  .mock-process__steps h3 {
    margin: 0;
    font-size: clamp(1.45rem, 2.2vw, 2.15rem);
    font-weight: 400;
    letter-spacing: -0.035em;
  }

  .mock-process__steps p {
    max-width: 34rem;
    margin: 0.75rem 0 0;
    color: var(--muted);
    font-size: 0.95rem;
    font-weight: 300;
    line-height: 1.7;
  }

  /* Contact */
  .mock-contact {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    color: #f4f0e8;
    background:
      radial-gradient(ellipse 80% 55% at 50% -10%, rgba(255, 255, 255, 0.05) 0%, transparent 55%),
      #202220;
    padding-block: clamp(5rem, 9vw, 8rem);
  }

  .mock-contact::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.45;
    background-image: var(--grain);
    background-size: 110px 110px;
    mix-blend-mode: overlay;
  }

  .mock-contact > * {
    position: relative;
    z-index: 1;
  }

  .mock-contact__inner {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(16rem, 0.7fr);
    gap: clamp(2.5rem, 8vw, 9rem);
    align-items: end;
  }

  .mock-contact .mock-kicker,
  .mock-contact__copy > p {
    color: color-mix(in srgb, #f4f0e8 62%, transparent);
  }

  .mock-contact h2 {
    color: inherit;
    max-width: 16ch;
  }

  .mock-contact__copy {
    display: grid;
    gap: 1.6rem;
    justify-items: start;
  }

  .mock-contact__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1.25rem;
  }

  @media (max-width: 980px) {
    .mock-skills__groups,
    .mock-about__capabilities,
    .mock-about__layout,
    .mock-process__layout,
    .mock-contact__inner,
    .mock-section-head,
    .mock-feature > a,
    .mock-pair {
      grid-template-columns: 1fr;
    }

    .mock-about__photo {
      max-width: 22rem;
    }

    .mock-feature__visual {
      aspect-ratio: 16 / 10;
    }

    .mock-process__intro {
      position: static;
    }

    .mock-refinement {
      left: auto;
      right: -9%;
      top: 53%;
      bottom: auto;
      width: min(61vw, 38rem);
      height: auto;
      transform: translateY(-50%);
    }

    .mock-refinement img {
      transform: none;
      height: 100%;
    }
  }

  @media (max-width: 680px) {
    .mock-hero {
      min-height: auto;
    }

    .mock-hero h1 {
      font-size: clamp(2.7rem, 12vw, 4rem);
    }

    .mock-refinement {
      position: relative;
      left: auto;
      right: auto;
      top: auto;
      bottom: auto;
      width: 112%;
      height: auto;
      margin: 1.25rem -6% -1rem;
      transform: none;
      opacity: 0.72;
    }

    .mock-hero__grid {
      flex-direction: column;
      align-items: stretch;
    }

    .mock-hero__intro,
    .mock-actions {
      background: none;
      padding-right: 0;
    }

    .mock-hero__foot {
      flex-direction: column;
      align-items: flex-start;
    }

    .mock-skills__groups {
      grid-template-columns: 1fr;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .mock *,
    .mock *::before,
    .mock *::after {
      transition-duration: 0.01ms !important;
    }

    .mock [data-reveal] {
      opacity: 1;
      transform: none;
      transition: none;
    }

    .mock [data-reveal-hero-media] {
      opacity: 1;
      transform: translateY(-50%);
    }
  }

  /* Scroll reveals */
  .mock [data-reveal] {
    opacity: 0;
    transform: translateY(0.85rem);
    transition:
      opacity 820ms cubic-bezier(0.22, 0.61, 0.36, 1),
      transform 820ms cubic-bezier(0.22, 0.61, 0.36, 1);
  }

  .mock [data-reveal].is-in {
    opacity: 1;
    transform: none;
  }

  .mock [data-reveal-media] {
    transform: translateY(0.45rem);
    transition:
      opacity 1100ms cubic-bezier(0.16, 0.7, 0.3, 1),
      transform 1100ms cubic-bezier(0.16, 0.7, 0.3, 1);
  }

  .mock [data-reveal-delay='1'] {
    transition-delay: 100ms;
  }

  .mock [data-reveal-stagger] > [data-reveal]:nth-child(1) {
    transition-delay: 0ms;
  }

  .mock [data-reveal-stagger] > [data-reveal]:nth-child(2) {
    transition-delay: 90ms;
  }

  .mock [data-reveal-stagger] > [data-reveal]:nth-child(3) {
    transition-delay: 180ms;
  }

  .mock [data-reveal-stagger] > [data-reveal]:nth-child(4) {
    transition-delay: 270ms;
  }

  .mock [data-reveal-hero] {
    transform: translateY(0.9rem);
  }

  /* Keep absolute vertical centering while the hero graphic reveals */
  .mock [data-reveal-hero-media] {
    transform: translateY(calc(-50% + 0.9rem));
  }

  .mock [data-reveal-hero-media].is-in {
    transform: translateY(-50%);
  }

  @media (max-width: 680px) {
    .mock [data-reveal-hero-media] {
      transform: translateY(0.9rem);
    }

    .mock [data-reveal-hero-media].is-in {
      transform: none;
      opacity: 0.72;
    }

    @media (prefers-reduced-motion: reduce) {
      .mock [data-reveal-hero-media] {
        transform: none;
        opacity: 0.72;
      }
    }
  }
</style>
