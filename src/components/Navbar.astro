---
import { nav, primaryCta } from '../config/site';

const path = Astro.url.pathname.replace(/\/+$/, '') || '/';
const isActive = (href: string) => (href === '/' ? path === '/' : path === href || path.startsWith(`${href}/`));
const isHome = path === '/';

const mainLinks = nav;
---

<header class:list={['site-nav', isHome && 'site-nav--home']} data-site-nav>
  <nav class="site-nav__inner" aria-label="Primary navigation">
    <a class="site-nav__brand" href="/">
      <span class="site-nav__wordmark" aria-label="Layer">
        layer<b>.</b>
      </span>
    </a>

    <div class="site-nav__links">
      {mainLinks.map((item) => (
        <a href={item.href} aria-current={isActive(item.href) ? 'page' : undefined}>{item.label}</a>
      ))}
    </div>

    <div class="site-nav__actions">
      <a class="site-nav__cta" href={primaryCta.href}>
        {primaryCta.label} <span aria-hidden="true">↗</span>
      </a>
      <details class="site-nav__menu">
        <summary>Menu</summary>
        <div>
          {nav.map((item) => (
            <a href={item.href} aria-current={isActive(item.href) ? 'page' : undefined}>{item.label}</a>
          ))}
          <a class="site-nav__menu-cta" href={primaryCta.href}>{primaryCta.label}</a>
        </div>
      </details>
    </div>
  </nav>
</header>

<style>
  .site-nav {
    --nav-paper: var(--ml-paper, #f7f8fa);
    --nav-ink: var(--ml-ink, #12151a);
    --nav-soft: var(--ml-ink-soft, #3a4250);
    --nav-muted: var(--ml-muted, #5c6570);
    --nav-line: var(--ml-line, rgba(18, 21, 26, 0.1));
    --nav-shell: var(--ml-shell, min(92rem, calc(100% - 3rem)));
    position: sticky;
    top: 0;
    z-index: 60;
    background: color-mix(in srgb, #f6f4ef 90%, transparent);
    border-bottom: 1px solid var(--nav-line);
    backdrop-filter: blur(18px) saturate(120%);
    font-family: 'IBM Plex Sans', system-ui, sans-serif;
    transition: background 220ms ease, border-color 220ms ease;
  }

  .site-nav__inner {
    width: var(--nav-shell);
    min-height: 4.75rem;
    margin-inline: auto;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 2rem;
  }

  .site-nav__brand {
    justify-self: start;
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    line-height: 1.15;
  }

  .site-nav__wordmark {
    color: var(--nav-ink);
    font-size: 1.65rem;
    font-weight: 450;
    letter-spacing: -0.065em;
  }

  .site-nav__wordmark b {
    color: #1d3557;
    font-weight: 700;
    letter-spacing: 0;
  }

  .site-nav__links {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
  }

  .site-nav__links a,
  .site-nav__menu a {
    color: var(--nav-soft);
    font-size: 0.94rem;
    text-decoration: none;
  }

  .site-nav__links a:hover,
  .site-nav__menu a:hover {
    color: var(--nav-ink);
  }

  .site-nav__links a[aria-current='page'],
  .site-nav__menu a[aria-current='page'] {
    color: var(--ml-system, #1b4f72);
    font-weight: 500;
  }

  .site-nav__actions {
    justify-self: end;
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .site-nav__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.62rem 1.05rem;
    border-radius: 0.3rem;
    color: #fff;
    background: #1d3557;
    font-size: 0.86rem;
    font-weight: 500;
    white-space: nowrap;
    text-decoration: none;
    transition: transform 200ms cubic-bezier(0.22, 1, 0.36, 1), box-shadow 200ms ease;
  }

  .site-nav__cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 8px 20px rgba(29, 53, 87, 0.2);
  }

  .site-nav__menu {
    display: none;
    position: relative;
  }

  .site-nav__menu summary {
    color: var(--nav-ink);
    font-size: 0.85rem;
    cursor: pointer;
    list-style: none;
  }

  .site-nav__menu summary::-webkit-details-marker {
    display: none;
  }

  .site-nav__menu > div {
    position: absolute;
    top: 2.2rem;
    right: 0;
    min-width: 13rem;
    display: grid;
    padding: 0.7rem;
    background: var(--ml-paper-raised, #faf7f1);
    border: 1px solid var(--nav-line);
    box-shadow: 0 18px 50px rgba(32, 30, 25, 0.12);
  }

  .site-nav__menu a {
    padding: 0.7rem 0.8rem;
  }

  .site-nav__menu-cta {
    margin-top: 0.35rem;
    color: #fff !important;
    background: #1d3557;
    font-weight: 500;
  }

  @media (max-width: 1100px) {
    .site-nav__links {
      gap: 1.35rem;
    }

    .site-nav__cta {
      font-size: 0.8rem;
      padding: 0.58rem 0.9rem;
    }
  }

  @media (max-width: 980px) {
    .site-nav__inner {
      grid-template-columns: 1fr auto;
      width: min(100% - 2rem, 60rem);
    }

    .site-nav__links {
      display: none;
    }

    .site-nav__menu {
      display: block;
    }
  }

  @media (max-width: 560px) {
    .site-nav__cta {
      display: none;
    }
  }

  .site-nav--home:not(.is-solid) {
    background: transparent;
    border-bottom-color: transparent;
    backdrop-filter: none;
  }

  .site-nav--home:not(.is-solid) .site-nav__wordmark {
    color: #fff;
  }

  .site-nav--home:not(.is-solid) .site-nav__wordmark b {
    color: #6eb4e8;
  }

  .site-nav--home:not(.is-solid) .site-nav__links a,
  .site-nav--home:not(.is-solid) .site-nav__menu summary {
    color: rgba(244, 245, 247, 0.72);
  }

  .site-nav--home:not(.is-solid) .site-nav__links a:hover,
  .site-nav--home:not(.is-solid) .site-nav__menu summary:hover {
    color: #fff;
  }

  .site-nav--home:not(.is-solid) .site-nav__links a[aria-current='page'] {
    color: #fff;
  }

  .site-nav--home:not(.is-solid) .site-nav__cta {
    color: #10233e;
    background: #f8f6f1;
  }

  .site-nav--home:not(.is-solid) .site-nav__cta:hover {
    background: #fff;
    box-shadow: none;
  }
</style>

{isHome && (
  <script>
    const nav = document.querySelector<HTMLElement>('[data-site-nav]');
    const hero = document.querySelector('.layer-hero');
    if (nav && hero) {
      const sync = () => {
        const heroBottom = hero.getBoundingClientRect().bottom;
        const navBottom = nav.getBoundingClientRect().bottom;
        nav.classList.toggle('is-solid', heroBottom <= navBottom + 12);
      };
      sync();
      window.addEventListener('scroll', sync, { passive: true });
      window.addEventListener('resize', sync);
    }
  </script>
)}
